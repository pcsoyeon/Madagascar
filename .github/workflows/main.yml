# This is a basic workflow to help you get started with Actions
name: Swift

on:
  push:
    branches: [ feature/*, main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: |
        pod install --repo-update --clean-install --project-directory=GitAction-Demo/
        xcodebuild test -workspace './GitAction-Demo/GitAction-Demo.xcworkspace' -scheme 'GitAction-DemoTests' -destination 'platform=iOS Simulator,name=iPhone 11 Pro Max,OS=13.0'
